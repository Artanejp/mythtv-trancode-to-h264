## Setting files for mythtv-reload-metadatas.sh .

# SET NOT 0 to REPLACE FILENAME with HEADER.
# SET 0 to APPEND HEADER to FILENAME.
REPLACE_HEADER=0

### About HEAD TITLE
HEAD_TITLE="Sample String(foo)[bar]"
META_TITLE="Sample String(foo)[bar]"

COMMENTS="
ANY COMMENTS

"

# MAY SET BELOW VALUES:
# "NONE"      : Don't append/replace header title
# "NUMERIC"   : HEAD TITLE with episode-number (automatically increments)
#               ,as "#xxx" .
#               SHOULD SET EPISODE_NUM.
# "HEAD_ONLY" : HEAD TITLE without episode-number.
APPEND_HEADER="NUMERIC"

# Initial episode number if needed.
EPISODE_NUM=1

# Use episode list file if set to below, filename.
EPISODES_LIST_FILE=""

# Around I/O priority.
IONICE_VALUE=7

# Around process priority.
NICE_VALUE=19


# Around encoding threads (for CODEC)
POOL_THREADS=6
FRAME_THREADS=6


# Around encoding threads (for FFMPEG/VIDEO FILTERS)
FILTER_THREADS=12
FILTER_COMPLEX_THREADS=12
FFMPEG_THREADS=6

# Set Codec type.
# Avaiables are : "HEVC" , "SVTAV1" and "RAV1E" .
# Using libx265   when setting "HEVC".
# Using libsvtav1 when setting "AV1".
#VCODEC_TYPE="AV1"
VCODEC_TYPE="HEVC"

## Only for SVT-AV1. See documentations.
#SVTAV1_DISABLE_TEMPORAL_FILTERING=1

# Encode preset value
# Encode preset value
#PRESET_VALUE="ultrafast"
#PRESET_VALUE="superfast"
#PRESET_VALUE="veryfast"
PRESET_VALUE="faster"

#PRESET_VALUE="fast"
#PRESET_VALUE="medium"
#PRESET_VALUE="slow"
#PRESET_VALUE="slower"
#PRESET_VALUE="veryslow"
#PRESET_VALUE="placebo"

### Tuning preset if needs.
#TUNE_VALUE=grain
#TUNE_VALUE=animation
#TUNE_VALUE=nograin # Only for AV1
#TUNE_VALUE=mid_grain # Only for AV1
#TUNE_VALUE=anime_grain # Only for AV1


# Quant parameters
CRF_VALUE=21.0
#CRF_MIN=15.0
#CRF_MAX=28.0

AQ_VALUE=0.75
AQ_MODE=4
QP_ADAPTATIVE_VALUE=1.20

# Rate control method (still only for libsvtav1).
RC_MODE=1

# TARGET BITRATE (VBR or CRF) / LIMIT BITRATE (CBR) by KiB
# If not set (or lower than 0), don't set this value.
TARGET_BITRATE_KBIT=2500

# Encode parameters (mostly not need to change)
#VBV_VALUE=3000


# Maximum prefetch data size (by MiB)
PREFETCH_FILE=2048

# Encode with 10bit depth.(main10).
# IF set to 0, encode with 8bit depth(main).
USE_10BIT=1

#HDR Setting (WIP, DO NOT SET)
USE_HDR10=0

## Subtitle (Closed Caption)
# If set to 1, dump subtext from sorurce.
#DUMP_SUB_FROM_SOURCE=0

# Appen subtitle track if exists at source.
#ADD_SUB_IF_EXISTS=1


# Only for Matroska
FFMPEG_APPEND_ARGS_POST+=("-cluster_time_limit")
FFMPEG_APPEND_ARGS_POST+=(125)

# Initial (prefix) filter string. See ffmpeg's -vf option.
FILTER_STRING=""

# Set 1 if use mythtv's database.
USE_DATABASE=0

# Set 1 if encode with forced FPS.
## IF both 0 then passthrough frame rate.
FORCE_FPS=0
DETECT_VFR=0
#BASE_FPS="60000/1001"


# Using stream number (of FFMPEG) if has multiple video tracks.
#VIDEO_STREAM="0:0"

# Set 1 to copy all audio tracks, 0 to re-encode all audio tracks.
COPY_AUDIOS=0
# Audio codec when re-encoding .
AUDIO_CODEC="aac"
# Encode parameter (see FFMpeg's manuals) of audio codec
AUDIO_ARGS="-ar 48000 -ab 224k"

# Commands
#NICE_CMD="/bin/nice"
#RENICE_CMD="/bin/renice"
#IONICE_CMD=/bin/ionice

#FFMPEG_CMD="/usr/bin/ffmpeg"
#FFMPEG_SUBTXT_CMD="${FFMPEG_CMD}"

    
